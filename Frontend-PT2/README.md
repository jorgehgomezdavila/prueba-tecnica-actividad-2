# Frontend-PT2 (Angular + Tailwind)

Proyecto base generado **como si** se hubiera creado con:

```bash
ng new Frontend-PT2 --style=css --routing --ssr=false
```

y luego se le agregó TailwindCSS.

## Instalación
```bash
npm install
```

## Desarrollo
```bash
npm start
```
